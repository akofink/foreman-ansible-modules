---
- hosts: localhost
  tasks:
  - name: "Create a compute profile"
    foreman_compute_profile:
      username: "admin"
      password: "changeme"
      server_url: "https://foreman.example.com"
      verify_ssl: false
      name: "Enterprise"
      state: "present"
    foreman_compute_profile:
      username: "admin"
      password: "changeme"
      server_url: "https://foreman.example.com"
      verify_ssl: false
      name: "Enterprise"
      state: "absent"
